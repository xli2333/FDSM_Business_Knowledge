# 复旦商业知识库 — 前端上线前文字审计报告

- **审计日期**：2026-04-17
- **审计范围**：frontend/src 下全部 .jsx / .js 文件中用户可见的中英文文案
- **审计目标**：确保游客、免费会员、付费会员看到的所有文字都适合外部正式用户，不包含内部/开发/产品术语

---

## 目录

1. 分类一：全局布局（Navbar / Footer / index.html）
2. 分类二：i18n 国际化文案（messages.js）
3. 分类三：首页（HomePage）
4. 分类四：搜索页（SearchPage / SearchBar）
5. 分类五：文章详情页（ArticlePage / ArticleCard）
6. 分类六：会员体系页面（MembershipPage / LoginPage / AuthDialog）
7. 分类七：栏目与专题页面（ColumnPage / TopicPage / TopicsPage / TagPage）
8. 分类八：媒体中心（MediaHubPage / MediaDetailPage）
9. 分类九：AI 助理与知识库（ChatPage / ChatPanel / 知识库系列）
10. 分类十：用户中心（MyLibraryPage / MyFollowingPage）
11. 分类十一：机构页面（OrganizationsPage / OrganizationPage）
12. 分类十二：时光机（TimeMachinePage）
13. 分类十三：商业与商务页面（CommercialPage / CommercialLeadsPage）
14. 分类十四：权限控制与角色配置（roleExperience.js / ProtectedRoute）
15. 分类十五：管理后台页面安全性检查

---

## 分类一：全局布局

### 1.1 index.html

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 页面标题是默认的脚手架名 | `index.html:7` `<title>` | `frontend` | `复旦商业知识库` 或 `Fudan Business Knowledge Base` | **高** |
| favicon 还是 Vite 默认图标 | `index.html:4` | `vite.svg` | 替换为复旦管院官方 logo 或自定义 favicon | **高** |
| html lang 属性为 en | `index.html:2` | `lang="en"` | 建议改为 `lang="zh-CN"` 或在 JS 中动态切换 | 中 |

### 1.2 Footer

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 底部署名像内部设计标注 | `Footer.jsx:22` | `Fudan Blue × Fudan Orange` | 改为 `复旦大学管理学院` 或 `© 2026 复旦管理学院` 等正式版权声明 | **高** |

### 1.3 Navbar

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| appSubtitle 偏内部英文产品定位 | `messages.js:4` | `Business-Only Knowledge Base` | 建议改为 `复旦大学管理学院出品` 或英文 `Powered by Fudan School of Management` | **高** |
| 导航栏 "登录中心" 略显内部 | `Navbar.jsx:127, 185` | `登录中心` | 改为 `登录` | 低 |
| 管理员导航含"内容运营"、"RAG 后台"、"销售线索" | `Navbar.jsx:88-98` | 管理员可见项 | 虽然仅管理员可见，但建议文案更正式：`RAG 后台` → `知识检索管理`，`销售线索` → `商务管理` | 中 |
| 导航栏"文章后台"、"媒体后台" | `Navbar.jsx:95-97` | `文章后台` / `媒体后台` | 仅管理员可见，但建议统一为 `内容管理` / `媒体管理` | 低 |
| 免费会员导航显示"商业方案" | `Navbar.jsx:123` | `商业方案` | 改为 `合作咨询` 或 `商务合作` 更正式 | 低 |

---

## 分类二：i18n 国际化文案

### 2.1 messages.js 中的问题

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| appSubtitle 中英文一样 | `messages.js:4,98` | `Business-Only Knowledge Base` | 中文改为 `复旦大学管理学院出品`，英文改为 `Powered by Fudan School of Management` | **高** |
| "读者数据" 像内部数据面板标题 | `messages.js:41` | `读者数据` / `Reader Pulse` | 改为 `阅读统计` / `Reading Stats` | 中 |
| "互动" 过于简略 | `messages.js:45` | `互动` / `Actions` | 改为 `文章互动` / `Article Actions` | 低 |
| "会员入口" 像内部导航 | `messages.js:69` | `会员入口` / `Membership` | 改为 `了解更多` / `Learn More` | 低 |
| "会员页会展示当前身份..." 像内部说明 | `messages.js:71` | `会员页会展示当前身份、可访问内容和升级路径，帮助你继续阅读更完整的内容。` | 改为 `查看您的会员权益，了解如何获取更完整的阅读内容。` | 中 |

---

## 分类三：首页

### 3.1 首页固定数字

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 固定写死文章数量 | `HomePage.jsx:343-344` | `基于复旦商业内容库中的 2142 篇文章` / `Built on 2,142 curated articles` | 应该从后端动态获取文章总数，而非硬编码。当前数字如果过时会显得不专业 | **高** |

### 3.2 首页产品/技术术语

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "Fudan Business Knowledge" 为英文品牌标语 | `HomePage.jsx:317` | `Fudan Business Knowledge` | 中文语境下建议改为 `复旦商业知识` 或直接用中文 | 低 |
| "产品价值" 像内部产品文档 | `HomePage.jsx:523` | `产品价值` / `Product Value` | 改为 `平台特色` / `Platform Features` | **高** |
| "立即体验" 适合，但区块内有些卡片名偏内部 | `HomePage.jsx:541` | `立即体验` / `Try It Now` | OK, 但下方的 `统一搜索` 建议改为 `智能搜索` 或 `全站搜索` | 低 |
| "会员访问" 描述像产品规格 | `HomePage.jsx:160` | `文章和音频已经支持公开、会员和付费可见性。` | 改为 `文章和音频支持多级访问权限，会员可享受更多内容。` | 中 |
| "合作与方案" 适合，但子描述过于内部 | `HomePage.jsx:563-564` | `你可以在商业页面查看产品能力、合作信息和联系入口，用于进一步沟通。` | 改为 `了解我们的合作方案和联系方式，欢迎洽谈合作。` | 中 |
| "Featured Story" 在无封面图时显示 | `HomePage.jsx:433` | `Featured Story` | 仅在无封面时显示且不影响太大，但建议中文语境加判断展示 `精选内容` | 低 |

### 3.3 首页角色面板措辞

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "管理员入口" 出现在首页 | `HomePage.jsx:176` | `管理员入口` | 虽然仅管理员可见，文案 OK | 低 |
| "在一个控制台里管理成员、内容和审核" | `HomePage.jsx:177` | 管理员面板标题 | 建议改为 `管理中心` 等更简洁的表述 | 低 |
| 管理员面板描述过于技术化 | `HomePage.jsx:179-180` | `登录后，管理员会直接进入会员管理、编辑工作、媒体后台和审计查看。` | 改为 `管理会员、内容编辑、媒体和运营数据。` | 低 |

---

## 分类四：搜索页

### 4.1 SearchPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "智能 RAG 搜索" 暴露技术实现 | `SearchPage.jsx:218` | `智能 RAG 搜索仅对付费会员开放` | 改为 `智能搜索仅对付费会员开放` —— 去掉 "RAG"，外部用户不需要知道技术架构 | **高** |
| 英文版同样暴露 RAG | `SearchPage.jsx:217` | `Smart RAG search is reserved for paid members.` | 改为 `Smart search is reserved for paid members.` | **高** |

### 4.2 SearchBar

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 无严重问题 | — | — | SearchBar 文案整体合适 | — |

---

## 分类五：文章详情页

### 5.1 ArticlePage — 高优先级

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 翻译元信息泄露 | 行672-680 | 显示 Cached 和 model 名 | 加 isAdmin 判断或彻底移除 | **高** |
| 英文正文区内部描述 | 行689 | Template HTML keeps... | 改为 English version follows the same layout. | **高** |

### 5.1 ArticlePage — 中低优先级

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| AI 摘要区描述偏内部 | 行625 | 沿用站内阅读布局展示 AI 摘要 | 改为 AI 自动生成的内容摘要，或去掉 | 中 |
| 英文摘要区同上 | 行625 | Same reading layout, localized... | 改为 AI-generated summary in English | 中 |
| 正文区排版说明 | 行689 | 正文区仅调整排版，不改变原有页面结构 | 改为 全文内容 或去掉 | 中 |
| 打回草稿箱按钮 | 行705 | 管理员可见 | 已有 isAdmin 判断，OK | 低 |
| 默认机构名 Business | 行509 | fallback 'Business' | 改为 复旦管院 或留空 | 中 |

### 5.2 ArticleCard

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 文章类型默认 "Business" | `ArticleCard.jsx:37` | `article.article_type \|\| 'Business'` | 建议改为 `商业` 或 `文章`，或改为空字符串 | 低 |

---

## 分类六：会员体系页面

### 6.1 MembershipPage — 环境与支付术语

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "当前环境" 多次出现 | 多处 | `当前环境支持创建订阅与订单记录` 等 | "当前环境" 是开发术语。改为 `支持创建订阅与查看订单记录` | **高** |
| "This environment..." 英文同 | 同上 | `This environment can create subscription...` | 改为 `You can create subscriptions and view order records.` | **高** |
| "支付环境" 字段标签 | 行394 | `支付环境` / `Payment path` | 改为 `支付方式` / `Payment method` | **高** |
| "方案确认" mock 支付 | 行143 | `方案确认` / `Plan review` | 未开启支付时显示 `待开通` | **高** |
| "尚未开启真实支付" | 行169 | checkout 结果 | 改为 `已记录您的方案选择，将尽快联系完成开通。` | **高** |
| "当前环境已开启/尚未开启..." | 行511-513 | 支付状态描述 | 改为 `在线支付已开放` / `在线支付即将开放` | **高** |

### 6.1 MembershipPage — 调试信息与产品设计泄露

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 账户面板泄露技术字段 | 行723-727 | `会员内容访问: 是/否` 等 | 去掉布尔值暴露，只展示等级和到期时间 | **高** |
| "账单侧" 术语 | 行743 | `账单侧当前没有激活中的订阅记录` | 改为 `当前没有激活的订阅` | 中 |
| "不再看到大块方案售卖区" | 行365 | UI 设计决策外露 | 改为 `查看您的当前权益和升级选项。` | **高** |
| "退化成售卖页" | 行371 | 产品设计讨论泄露 | 改为 `管理您的方案、订阅状态和续费操作。` | **高** |
| "管理员权限不通过售卖获得" | 行376 | 管理员描述 | 仅管理员可见，建议改为更正式表述 | 低 |
| "旧的四条音频入口" | 行254 | 版本迭代内部语境 | 改为 `直接进入完整音频内容。` | **高** |
| 英文同: "old four-track entry" | 同上 | `instead of the old four-track entry` | 改为 `Go straight into the full audio stream.` | **高** |

### 6.2 LoginPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 无严重问题 | — | LoginPage 文案整体得体 | — | — |

### 6.3 AuthDialog

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 无严重问题 | — | AuthDialog 文案整体得体 | — | — |

---

## 分类七：栏目与专题页面

### 7.1 TopicPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 未做国际化 - 多处硬编码中文 | `TopicPage.jsx:142,156,158,164,177,179,186,199` | `专题阅读`、`已关注专题`、`关注专题`、`相关文章`、`加载中...`、`继续加载`、`时间线`、`AI 洞察` | 这些文字没有做 isEnglish 判断。当用户切换到英文模式时仍显示中文。建议补全英文翻译 | **高** |

### 7.2 TopicsPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "专题页已收紧到付费会员与管理员" | `TopicsPage.jsx:58` | `专题页已收紧到付费会员与管理员。登录后系统会按你的当前身份判断是否可读。` | "收紧" 是产品内部决策术语。改为 `专题仅对付费会员和管理员开放。登录后系统会自动判断您的访问权限。` | 中 |
| 英文同 "Topics are now limited to..." | `TopicsPage.jsx:58` | `Topics are now limited to paid members and admins.` | "now limited" 暗示原来不限制。改为 `Topics are available to paid members and admins.` | 中 |
| "专题详情页已收紧到付费会员与管理员" | `TopicPage.jsx:106` | 同上 | 同上处理 | 中 |

### 7.3 ColumnPage / TagPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 无严重问题 | — | 文案整体合适 | — | — |

---

## 分类八：媒体中心

### 8.1 MediaHubPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 音频页标题像内部产品日志 | `MediaHubPage.jsx:26` | `音频流改成发现页，完整内容进入正式详情页` | 这是产品迭代日志式描述。改为 `发现并聆听精选商业音频内容` | **高** |
| 英文同样问题 | `MediaHubPage.jsx:33` | `Audio now works as a discovery page with dedicated detail pages` | 改为 `Discover and listen to curated business audio content` | **高** |
| 音频页正文描述像内部产品说明 | `MediaHubPage.jsx:27` | `音频流现在只负责浏览与进入详情。每条内容进入单独正式页后，再查看摘要、节目简介、章节目录与播放器。` | 改为 `浏览音频内容，点击任一节目进入详情页查看完整信息与播放。` | **高** |
| 视频页同样问题 | `MediaHubPage.jsx:42-44` | `视频流回到列表职责，完整信息进入正式详情页` / `视频流现在只负责内容发现和详情入口。` | 改为 `发现精选商业视频内容` / `浏览视频内容，点击任一视频进入详情页观看。` | **高** |
| "付费音频流" kicker | `MediaHubPage.jsx:25` | `付费音频流` / `Premium Audio Stream` | 改为 `音频频道` / `Audio Channel` —— 不是所有音频都付费，且 "流" 是技术术语 | 中 |
| "视频流" kicker | `MediaHubPage.jsx:41` | `视频流` / `Video Stream` | 改为 `视频频道` / `Video Channel` | 中 |

### 8.2 MediaDetailPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "时长待补全" | `MediaDetailPage.jsx:24` | `时长待补全` / `Duration pending` | 如果时长未填，最好不显示这个字段，或改为 `时长未知` / `Duration unknown` | 中 |
| "媒体文件上传后会在这里出现播放器" | `MediaDetailPage.jsx:255` | 无媒体文件时的提示 | 这是给编辑/管理员看的。建议对非管理员显示 `该内容正在准备中，请稍后再来。` | **高** |

---

## 分类九：AI 助理与知识库

### 9.1 ChatPanel

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 无严重问题 | — | ChatPanel 文案整体合适 | — | — |

### 9.2 UserKnowledgeHubPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "Knowledge Console" kicker | `UserKnowledgeHubPage.jsx:29` | `Knowledge Console` / `知识库工作台` | "Console" / "工作台" 偏内部。建议改为 `My Knowledge Base` / `我的知识库` | 中 |

### 9.3 MyLibraryPage 知识库卡片

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "stock portfolio 知识库模式" | 行60 | 纯内部产品设计笔记 | 改为 创建私有主题，归档文章，用 AI 分析 | **高** |
| 英文同 "stock-portfolio pattern" | 行60 | 同上英文版 | 改为 Create private themes, file articles, use AI. | **高** |

---

## 分类十：用户中心

### 10.1 MyLibraryPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "全部资产流" | `MyLibraryPage.jsx:146` | `全部资产流` / `All flows` | "资产流" 偏产品术语。改为 `全部` / `All` | 中 |
| "阅读资产流" | `MyLibraryPage.jsx:352` | `阅读资产流` / `Reading flows` | 改为 `阅读记录` / `Reading History` | 中 |
| "收藏、点赞与历史放在同一个回流页" | `MyLibraryPage.jsx:353` | 回流页 | "回流页" 是产品术语。改为 `收藏、点赞与阅读历史` / `Bookmarks, likes, and reading history` | 中 |
| "阅读资产" kicker | `MembershipPage.jsx:466` | `阅读资产` / `Reading assets` | 改为 `阅读数据` / `Reading Data` | 低 |

### 10.2 MyFollowingPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "Personal Watchlist" kicker | `MyFollowingPage.jsx:165` | `Personal Watchlist` | 改为 `My Following` / `我的关注` | 低 |
| "左侧选择你要继续追踪的标签..." 描述过长且偏 UI 说明 | `MyFollowingPage.jsx:170` | `左侧选择你要继续追踪的标签、专题或栏目，右侧只显示该关注对象对应的文章。删除操作独立放在卡片右上角。` | 去掉 "删除操作独立放在卡片右上角" —— 这是 UI 布局说明，不是用户需要的信息。改为 `选择关注的标签、专题或栏目，查看对应的最新文章。` | 中 |

---

## 分类十一：机构页面

### 11.1 OrganizationsPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 未做国际化 - 全部硬编码中文 | `OrganizationsPage.jsx` 全文 | `机构索引`、`从文章中的机构字段聚合而来...`、`相关文章：X 篇`、`最近更新：` | 没有 isEnglish 判断，英文模式下仍显示中文 | **高** |
| 描述过于内部 | `OrganizationsPage.jsx:19` | `从文章中的机构字段聚合而来，用于把单篇内容继续串成机构维度的知识入口。` | 这是给内部团队解释功能来源的。改为 `按机构浏览相关文章，了解各大商业机构的研究与观点。` | **高** |
| 错误信息未做国际化 | `OrganizationsPage.jsx:10` | `机构列表加载失败` | 需要做 isEnglish 判断 | 中 |

### 11.2 OrganizationPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 未做国际化 - 全部硬编码中文 | `OrganizationPage.jsx` 全文 | `机构页加载中...`、`当前共聚合 X 篇相关文章，最近更新时间为...`、`加载中...`、`继续加载` | 没有 isEnglish 判断 | **高** |
| "Organization Hub" kicker 在中文页面出现英文 | `OrganizationPage.jsx:40` | `Organization Hub` | 应根据语言判断，中文显示 `机构主页` | 中 |
| "当前共聚合 X 篇相关文章" | `OrganizationPage.jsx:43` | `当前共聚合 X 篇相关文章` | "聚合" 偏技术。改为 `共 X 篇相关文章` | 低 |

---

## 分类十二：时光机

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 无严重问题 | — | TimeMachinePage 文案整体得体 | — | — |

---

## 分类十三：商业与商务页面

### 13.1 CommercialPage — 国际化与内部标签

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "Commercial Readiness" | 行126 | 内部产品成熟度描述 | 改为 Business Cooperation / 商务合作 | **高** |
| 多个 kicker 全用英文 | 行200等 | Capabilities / Trust Signals 等 | 做国际化：平台能力、数据实力、合作方式等 | **高** |
| "当前环境预发布方式" | 行236 | 暴露开发状态 | 改为 订阅功能即将上线 | **高** |

### 13.1 CommercialPage — 技术细节暴露

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "FAISS Index" / "AI Engine" | 行263,269 | 暴露向量索引名和引擎名 | 改为 知识检索引擎 / AI 能力 | **高** |
| "Gemini" 模型名暴露 | 行273 | Gemini 已接入摘要、查询扩展和重排 | 去掉模型名和技术术语 | **高** |
| "BM25 和向量检索" | 行83 | 搜索引擎技术名词 | 改为 智能建议和语义检索 | **高** |
| "HTML 渲染" | 行93 | 太技术 | 改为 排版 | 中 |
| "直接写入后台" | 行117 | 偏技术 | 改为 将由专人跟进 | 中 |
| "线索编号" | 行384 | CRM 术语 | 改为 申请编号 | 中 |

### 13.2 CommercialLeadsPage

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| 此页面受 ProtectedRoute requireAdmin 保护 | — | 仅管理员可访问 | 安全，无外部泄露风险 | — |

---

## 分类十四：权限控制与角色配置

### 14.1 roleExperience.js

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| guest quickAction 默认搜索链接带 mode=exact | `roleExperience.js:49` | `/search?q=AI&mode=exact` | 链接里固定用 `q=AI` 作为搜索词。用户点击后会看到预填的 "AI" 搜索。建议去掉 `q=AI`，让搜索页为空状态 | 中 |
| "进入我的资产" | `roleExperience.js:71` | `进入我的资产` / `Open my library` | "资产" 偏金融术语，不适合知识平台。改为 `进入我的资料库` / `Open my library` (英文 OK) | 中 |
| "系统会按你的偏好继续组织阅读" | `roleExperience.js:83` | `系统会按你的偏好继续组织阅读。` | "系统" 偏技术。改为 `平台会根据你的偏好推荐内容。` | 低 |
| "The product keeps shaping your reading around your interests." | `roleExperience.js:83` | 英文 | "The product" 偏内部。改为 `The platform shapes your reading around your interests.` | 低 |
| "公开、会员、付费媒体在同一系统内持续更新" | `roleExperience.js:139` | paid_member statCard | "同一系统内" 偏技术。改为 `持续更新的多级媒体内容。` | 低 |
| admin heroBody 提到 "收口" | `roleExperience.js:183` | `会员管理、文章工作台、媒体后台和商务线索都在这里收口。` | "收口" 是内部产品术语。改为 `...统一管理。` | 低（仅管理员可见）|

### 14.2 ProtectedRoute

| 问题 | 位置 | 当前文字 | 建议修改 | 严重程度 |
|------|------|---------|---------|---------|
| "Loading access..." | `ProtectedRoute.jsx:13` | `Loading access...` | 建议做国际化，中文模式显示 `正在加载...` | 中 |

---

## 分类十五：管理后台页面安全性检查

### 15.1 路由权限

| 页面 | 路由 | 保护方式 | 评估 |
|------|------|---------|------|
| AdminConsolePage | `/admin` | `requireAdmin` | 安全 |
| AdminMembershipsPage | `/admin/memberships` | `requireAdmin` | 安全 |
| RagAdminPage | `/admin/rag` | `requireAdmin` | 安全 |
| AdminContentOperationsPage | `/admin/content-ops` | `requireAdmin` | 安全 |
| EditorialWorkbenchPage | `/editorial` | `requireAdmin` | 安全 |
| MediaStudioPage | `/media-studio` | `requireAdmin` | 安全 |
| CommercialLeadsPage | `/commercial/leads` | `requireAdmin` | 安全 |
| AnalyticsPage | `/analytics` | `requireAdmin` | 安全 |

**结论**：所有管理后台页面均已通过 `ProtectedRoute requireAdmin` 保护。非管理员会被重定向，无法访问内容。

### 15.2 管理功能在非管理页面的泄露

| 位置 | 问题 | 当前表现 | 建议 |
|------|------|---------|------|
| `ArticlePage.jsx:697-707` | "打回草稿箱修改" 按钮 | 已有 `isAdmin` 判断 | 安全 |
| `MediaHubPage.jsx:228-234` | "媒体后台" 入口 | 已有 `isAdmin` 判断 | 安全 |
| `MediaDetailPage.jsx:382-392` | "重新编辑" 按钮 | 已有 `isAdmin` 判断 | 安全 |
| `MediaHubPage.jsx:157-165` | "Edit again" 按钮 | 已有 `canEdit` (=isAdmin) 判断 | 安全 |

---

## 汇总：按严重程度分类

### 高优先级（上线前必须修改）

1. **`index.html` 页面标题为 "frontend"** — 所有用户可见
2. **`index.html` favicon 为 Vite 默认** — 所有用户可见
3. **Footer "Fudan Blue × Fudan Orange"** — 像内部设计标注
4. **`messages.js` appSubtitle** — "Business-Only Knowledge Base" 偏内部
5. **首页硬编码 "2142 篇文章"** — 数字可能已过时
6. **首页 "产品价值" 标题** — 应改为"平台特色"
7. **搜索页暴露 "RAG"** — 技术术语
8. **文章页翻译元信息（Cached / model 名）泄露给所有用户**
9. **文章页正文区内部描述** — "Template HTML keeps the English article..."
10. **会员页多处 "当前环境" / "This environment"** — 开发术语
11. **会员页暴露 mock 支付提供商** — "方案确认"
12. **会员页 "尚未开启真实支付"** — 外部用户不应看到
13. **会员页产品设计讨论泄露** — "避免会员页再次退化成售卖页"
14. **会员页免费会员管理面板描述泄露** — "不再看到大块方案售卖区"
15. **会员页 "旧的四条音频入口"** — 版本迭代内部语境
16. **会员页账户状态面板泄露技术字段** — can_access_member 等
17. **TopicPage 未做国际化** — 英文模式显示中文
18. **MediaHubPage 音频/视频标题像产品日志** — "音频流改成发现页"
19. **MediaDetailPage 无文件时的编辑提示** — "媒体文件上传后会在这里出现播放器"
20. **MyLibraryPage "stock portfolio 的知识库模式"** — 纯内部设计笔记
21. **OrganizationsPage 全部未做国际化** — 英文模式显示中文
22. **OrganizationPage 全部未做国际化** — 英文模式显示中文
23. **OrganizationsPage 描述过于内部** — "从文章中的机构字段聚合而来"
24. **CommercialPage "Commercial Readiness"** — 内部产品成熟度
25. **CommercialPage kicker 全用英文未做国际化**
26. **CommercialPage 暴露 FAISS / Gemini** — 技术细节
27. **CommercialPage "BM25 和向量检索"** — 搜索技术名词
28. **CommercialPage "当前环境会先以预发布方式..."** — 开发环境状态

### 中优先级（建议在上线前修改）

1. messages.js "读者数据" → "阅读统计"
2. messages.js "会员页会展示当前身份..." 偏内部说明
3. 首页 "文章和音频已经支持公开、会员和付费可见性" 像产品规格
4. 首页 "产品能力、合作信息和联系入口" 描述偏内部
5. 文章页 AI 摘要区描述偏内部
6. 文章页默认机构名 "Business"
7. MediaHubPage kicker "付费音频流" / "视频流"
8. MediaDetailPage "时长待补全"
9. MyLibraryPage "全部资产流" / "阅读资产流" / "回流页"
10. MyFollowingPage UI 布局说明泄露
11. OrganizationPage "聚合" / "Organization Hub" kicker
12. CommercialPage "HTML 渲染" / "提交的信息会直接写入后台" / "线索编号"
13. TopicsPage "收紧" / "now limited"
14. UserKnowledgeHubPage "Knowledge Console"
15. roleExperience.js "我的资产" / "进入我的资产"
16. ProtectedRoute "Loading access..." 未国际化
17. 会员页 "账单侧" 术语
18. `index.html` lang="en"

### 低优先级（可后续优化）

1. Navbar "登录中心"
2. Navbar 管理员导航文案优化
3. Navbar 免费会员 "商业方案"
4. messages.js "互动" / "会员入口" 偏简略
5. 首页英文品牌标语、Featured Story
6. 首页管理员面板措辞
7. ArticleCard 默认 "Business" 类型
8. MyFollowingPage "Personal Watchlist"
9. roleExperience.js 多处细节措辞
10. MembershipPage 管理员描述

---

## 附录：需要补全国际化的页面清单

以下页面存在大量硬编码中文，在英文模式下无法正确显示：

| 页面 | 文件 | 严重程度 |
|------|------|---------|
| TopicPage | `src/pages/TopicPage.jsx` | 高 |
| OrganizationsPage | `src/pages/OrganizationsPage.jsx` | 高 |
| OrganizationPage | `src/pages/OrganizationPage.jsx` | 高 |
| CommercialPage (kicker 部分) | `src/pages/CommercialPage.jsx` | 高 |
| AnalyticsPage | `src/pages/AnalyticsPage.jsx` | 低（仅管理员可见）|

---

*报告完*
