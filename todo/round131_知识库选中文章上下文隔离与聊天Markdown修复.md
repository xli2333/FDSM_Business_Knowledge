# Round 131：知识库选中文章上下文隔离与聊天 Markdown 修复

## 对齐结论
- [x] 当前主题聊天虽然支持传递选中文章 ID，但聊天上下文仍按主题维度复用，切换选择后容易把旧范围里的回答继续带进来。
- [x] 主题聊天需要按“当前选中文章集合”隔离上下文，至少不能在单选文章时继续沿用多选文章的旧对话内容。
- [x] 当前 AI 返回的 Markdown 偶尔会带坏掉的 `**` 标记，前端直接渲染会留下肉眼可见的双星号。
- [x] 这一轮需要同时补后端防串台、前端上下文隔离、Markdown 清洗与真实验收。
- [x] 全部 todo 与代码改动保持 UTF-8 中文，不引入中文编码错误或 JSX 错误。

## 原子任务
- [x] P0 | Todo | 新建本轮中文 todo，并按原子项推进与勾选。
- [ ] P0 | 上下文隔离 | 主题聊天本地历史改为按主题 + 选中文章集合分别存储，切换选择时不复用旧范围消息。
- [ ] P0 | 提交防串台 | 发送消息时强制使用当前选择集对应的消息上下文，避免旧状态在异步切换瞬间混入请求。
- [ ] P0 | 后端历史收紧 | 主题聊天后端只保留必要的历史提问信息，避免旧回答反向污染当前检索范围。
- [ ] P0 | Markdown 修复 | 清洗主题聊天回答中的坏掉粗体标记，避免界面出现未渲染的双星号。
- [ ] P0 | 验收脚本 | 补真实验收，确认单选文章后来源只剩单篇，且聊天区不再出现裸露的 `**`。
- [ ] P0 | 测试 | 补后端测试覆盖聊天历史收紧与 Markdown 清洗。
- [ ] P0 | 验证 | 运行 `pytest backend/tests/test_user_knowledge_features.py -q`、`npm run build` 与知识库真实验收。

## 结束标准
- [ ] 单选文章后，主题聊天不再沿用旧多选上下文，也不再引用未选中的文章。
- [ ] 主题聊天回答中的坏掉 Markdown 标记会被清洗，界面不再出现裸露双星号。
- [ ] 真实验收和测试全部通过；若仍有明确提升空间，则继续开下一轮 Todo。
