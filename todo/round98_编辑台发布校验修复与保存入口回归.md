# Round 98：编辑台发布校验修复与保存入口回归
## 对齐规则
- [x] 发布前校验卡显示通过，但点击发布又报“最终 HTML 尚未生成”，这是严重的发布链路错误，必须优先修复。
- [x] “保存”按钮要回到最终稿编辑区附近，不能继续藏在远离编辑器的位置。
- [x] 本轮继续保持 UTF-8 中文，不引入中文编码或 JSX 问题。

## 本轮待办
- [x] P0 | 排查 | 核实发布按钮会先调用一次保存，而旧后端逻辑只要 payload 里出现标题/作者/机构/原稿等字段，就无条件把最终 HTML 清空，即使值根本没有变化。
- [x] P0 | 后端 | 修正编辑稿更新逻辑，只有真正发生了影响排版的字段变化时才让最终 HTML 失效，不能因为“保存同样的数据”就把 HTML 清掉。
- [x] P0 | 后端 | 补自动化测试，覆盖“相同元数据保存后仍可发布”和“真实改动排版敏感字段后应清空最终 HTML”两条路径。
- [x] P0 | 前端 | 将“保存”入口移到最终稿编辑区头部，提升可发现性。
- [x] P0 | 前端 | 精简顶部主操作区，移除远离编辑器的重复保存按钮，并在发布前同步最新保存结果，避免界面继续显示过期的校验状态。
- [x] P0 | 验收 | `pytest backend/tests/test_admin_editorial_media_features.py -q`
- [x] P0 | 验收 | `python -m compileall backend`
- [x] P0 | 验收 | `npm run build`

## 结束标准
- [x] 已通过校验的稿件，点击发布不会再因为一次无效保存把最终 HTML 清空。
- [x] 真正修改了标题、原稿、排版模式等敏感字段时，最终 HTML 仍会正确失效并要求重新排版。
- [x] 保存按钮在最终稿区域附近，用户不需要离开编辑器去找保存入口。

## 结论
- [x] Round 98 已修复发布前“假通过、真失败”的主链路 bug，并把保存入口移回最终稿区域，当前没有新的明确 P0 阻塞项。
