# Round144 公众号排版运行时去外部依赖迁移

- [x] 梳理自动排版正式链路，确认 `auto-format -> fudan_wechat_renderer -> wechat_fudan_bridge` 的调用边界与失败点。
- [x] 定位外部运行时真实源文件与依赖树，确认当前项目真正需要的运行时闭包。
- [x] 在主项目内部创建稳定的公众号排版运行时目录，承载正式运行代码。
- [x] 将外部服务文件迁入主项目内部，保留现有导出接口与行为。
- [x] 为内置运行时补齐独立 `package.json`、`package-lock.json` 与依赖安装，避免继续借用外部工程环境。
- [x] 改造 `wechat_fudan_bridge.mjs`，只引用主项目内部运行时，不再依赖 `公众号排版` 或其他外部目录。
- [x] 补充桥接层和 Python 调用层的健壮性处理，避免路径缺失时只暴露含糊 502。
- [x] 真实跑一遍编辑台自动排版链路，确认 `POST /api/editorial/articles/:id/auto-format` 已恢复成功。
- [x] 补自动化测试，覆盖桥接脚本已内置、自动排版返回真实公众号预览 HTML。
- [x] 跑后端相关测试与真实验收，确认迁移没有破坏编辑台预览、发布和历史渲染链路。
- [x] 本轮真实验收没有发现新的结构性缺口，不再开启下一轮 Todo。
