# round159 Gemini模型统一切换到3_0Flash
- [x] 排查项目中仍然残留的 `gemini-2.5-flash` 与 `gemini-2.5-pro` 运行时代码、测试和验收脚本。
- [x] 将运行时配置默认值统一切到 `gemini-3.0-flash`，并保留到 `gemini-3-flash-preview` 的兼容映射。
- [x] 增加旧环境变量兜底归一化，确保即使 `.env` 里还写着 `2.5` 也会自动切到 `3.0 flash`。
- [x] 更新批处理脚本、回归测试和验收脚本中的旧模型断言，避免再出现 `2.5` 口径。
- [x] 运行最小验证并回填 Todo，确认没有引入新的模型名错误或语法问题。
