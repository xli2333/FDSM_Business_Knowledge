# Round 107：媒体后台上传时保留本地草稿编辑态

## 对齐结论
- [x] `round106` 的媒体草稿流主链已经打通，后端测试、前端构建和 Playwright 媒体验收脚本都可运行。
- [x] 真实验收暴露出一处明确缺口：管理员先改标题等字段、再上传转录或脚本时，页面会被后端返回的旧草稿详情回刷，导致本地未保存编辑态丢失。
- [x] 这个问题属于媒体页上传动作没有先固化当前草稿，而不是后端发布链路缺失，因此需要单独一轮修正并复验。

## 原子任务
- [x] P0 | 前端状态 | 为媒体页上传动作补“上传前保存当前草稿”保护：已有草稿时先持久化当前表单，再执行主媒体 / 转录 / 脚本上传。
- [x] P0 | 前端状态 | 对于尚未创建草稿但本地已经填写了标题、来源、文案等字段的场景，上传前先创建草稿，避免上传完成后的详情回刷覆盖本地输入。
- [x] P0 | 前端验收 | 更新 `media_acceptance_round106.mjs`，固定覆盖“先改标题 -> 再传转录 -> 生成文案 -> 发布 -> 回编”的路径，确保标题不会在上传后丢失。
- [x] P0 | 验收 | 重新运行媒体验收脚本、后端媒体测试和前端构建，确认没有新增编码问题或 JSX 错误。

## 结束标准
- [x] 媒体后台在上传主媒体、转录、脚本时不再丢失本地未保存的标题等编辑字段。
- [x] `npm run media:acceptance:round106` 可以稳定通过，并能证明发布后的项目可从已发布列表回编到草稿箱。
- [x] 媒体草稿主链继续保持 `pytest` 通过、`npm run build` 通过。

## 备注
- 这轮只修上传导致的本地编辑态丢失，不重开 `round106` 已完成的数据模型与发布链路。
- 本轮验收后未再暴露出新的明确媒体工作流缺口，因此不再继续开 `round108`。
