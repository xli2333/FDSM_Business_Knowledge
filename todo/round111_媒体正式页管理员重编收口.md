# Round 111：媒体正式页管理员重编收口

## 对齐结论
- [x] 文章侧 `round93` 已经验证过“未发布只删、已发布自动离开草稿箱、需要修改时从正式页重新进入编辑流”这套语义，媒体侧本轮对齐到同一规则。
- [x] 媒体草稿发布成功后继续保持“自动从草稿箱删除”，不回退成已发布草稿留存模式。
- [x] 媒体草稿箱只承担“当前草稿编辑 + 删除”职责，不再承担“已发布媒体回编入口”职责。
- [x] 已发布音频 / 视频的重新编辑入口改放到正式展示页；管理员从正式展示页点击“重新编辑”后，再回到草稿箱继续编辑。
- [x] 本轮按现有 `/audio` 与 `/video` 作为“正式页面”处理，不新增独立媒体详情页。
- [x] 全部 todo 文案与代码改动保持 UTF-8 中文，不引入中文编码错误或 JSX 语法错误。

## 原子任务
- [x] P0 | 前端正式页 | 在 `/audio` 与 `/video` 的媒体卡片上增加管理员专属“重新编辑”入口，普通用户不显示。
- [x] P0 | 前端跳转 | 管理员点击“重新编辑”后，直接进入对应媒体草稿编辑态，而不是先回媒体后台列表再人工查找。
- [x] P0 | 前端工作台 | 为 `MediaStudioPage.jsx` 补从 URL 参数读取目标草稿并自动选中的能力，支持“从正式页进入后直达对应草稿”。
- [x] P0 | 前端工作台 | 删除媒体后台右侧“已发布媒体”列表及其“打回草稿箱”按钮，避免后台继续承担已发布媒体回编入口。
- [x] P0 | 前端文案 | 全量移除媒体链路里“打回草稿箱 / Send to draft box”对管理员可见的文案，统一改为“重新编辑 / Edit again”语义。
- [x] P0 | 前端危险操作 | 草稿箱里的媒体稿继续只保留删除动作，不新增或保留任何“打回 / 恢复 / 回编”类按钮。
- [x] P0 | 后端回编 | 保留已发布媒体复用既有草稿或新建关联草稿的能力，但对外语义收口为“重新编辑已发布媒体”。
- [x] P0 | 后端接口 | 下掉媒体后台旧的已发布库回编接口与对应前端依赖，避免“后台打回草稿箱”入口继续存在。
- [x] P0 | 回归验证 | 确认未发布媒体草稿可删除、已发布媒体发布后自动离开草稿箱、删除重编草稿不影响正式媒体内容三条规则继续成立。
- [x] P0 | 测试 | 更新后端媒体测试，覆盖已发布媒体重新编辑时复用既有草稿且不会生成重复稿。
- [x] P0 | 验收 | 更新 `frontend/scripts/media_acceptance_round106.mjs`，把验收路径改成“发布 -> 去正式 `/audio` 或 `/video` 页面 -> 管理员点击重新编辑 -> 直达草稿继续修改”。

## 结束标准
- [x] 媒体草稿箱只剩草稿编辑与删除职责。
- [x] 已发布音频 / 视频不再从媒体后台“打回草稿箱”，而是只能从正式展示页进入“重新编辑”。
- [x] 管理员从正式展示页进入编辑后，能直接落到对应草稿。
- [x] 媒体发布成功后仍自动从草稿箱删除。
- [x] `pytest backend/tests/test_admin_editorial_media_features.py -q`、`npm run build`、`npm run media:acceptance:round106` 通过。

## 备注
- [x] 本轮只收口媒体“重新进入编辑流”的入口位置与交互语义，不重做既有媒体草稿模型与发布模型。

## 结论
- [x] 真实验收未再暴露新的明确 P0 / P1 缺口，本轮到此收束，不新开下一轮中文 todo。
