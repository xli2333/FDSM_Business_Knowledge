# Round 133：知识库聊天 Markdown 原始格式归类与渲染修复

## 对齐结论
- [x] 当前主题聊天里残留的星号问题，不能继续靠猜测修正，必须先看后端 AI 返回的原始 Markdown。
- [x] 现有前后端都存在星号归一化逻辑，这层逻辑可能会把本来合法的 Markdown 改坏，需要先区分“模型原始输出问题”和“本地清洗问题”。
- [x] 本轮先按原始格式归类，再决定最小修复面，避免继续堆叠互相冲突的正则。
- [x] 所有修改与回填继续保持中文 Todo 勾选制，不引入中文编码错误或 JSX 错误。

## 原子任务
- [x] P0 | Todo | 新建本轮中文 Todo，并按原子项推进与回填。
- [x] P0 | 原始样本 | 直接从后端抓取知识库聊天的原始 AI Markdown，收集至少几条真实样本。
- [x] P0 | 格式归类 | 归类当前 AI 常见输出格式，明确标题、列表、强调、引用和来源等模式。
- [x] P0 | 失真定位 | 判断异常星号究竟来自模型原始输出还是前后端 Markdown 清洗逻辑。
- [x] P0 | 渲染修复 | 依据真实格式调整前端渲染或前后端归一化，移除会制造新残缺标记的修补逻辑。
- [x] P0 | 测试补强 | 补充后端或前端针对异常 Markdown 形态的测试，覆盖残缺星号和合法强调场景。
- [x] P0 | 验证 | 运行 `pytest backend/tests/test_user_knowledge_features.py -q` 与 `npm run build`，并完成知识库定点验收。

## 结束标准
- [x] 后端原始 Markdown 格式已看清，不再基于截图猜测修补。
- [x] 主题聊天区不再出现裸露星号或被截断的强调标记。
- [x] 合法的粗体、列表、标题和段落能按真实返回格式稳定渲染。
- [x] 验证通过；若仍有明确提升空间，则继续开启下一轮中文 Todo。
