# Round74 标签接口回归与最终验收收口

## P0 目标
- [x] 修复真实 smoke 暴露的 `/api/tags` 接口回归，不影响现有标签内容资产
- [x] 重新运行 smoke 验收，确认摘要 HTML 资产修复与标签接口同时通过
- [x] 复核数据库摘要 HTML 资产、摘要接口耗时与前端构建结果
- [x] 回填本轮结果，并判断是否还有明确提升空间

## 结果记录
- [ ] 若验收通过且无新的明确阻塞项，记录最终收口结论并结束推进
- [x] 若仍有新的明确阻塞项，继续新开下一轮中文 Todo 推进

## 本轮结果
- [x] `/api/tags` 已恢复，问题根因是 `catalog_service.list_tags()` 读取了未导入的 `TAG_CATEGORY_LABELS`
- [x] 重新跑 smoke 后，新的明确阻塞项转为“英文原稿发布后翻译接口仍尝试调用 AI，导致 503”
- [x] 数据库摘要 HTML 资产、摘要接口耗时与前端构建结果已复核，摘要修复链路本身保持通过
- [x] 该问题与摘要 HTML 资产修复不同，按规则继续新开下一轮 Todo 收口
