# Round 140：聊天 Markdown 表格统一渲染修复
## 对齐结论
- [x] 当前知识库聊天和 AI 助理仍然是两套 Markdown 渲染链路，表格没有统一按 GFM 规则解析。
- [x] 这轮必须抽出共用的 Markdown 归一化和渲染组件，不能继续在单个页面局部补丁。
- [x] 表格不仅要在知识库主题聊天里正确显示，也要在 AI 助理历史会话和新回复里走同样规则。
- [x] 验收不能只看截图主观判断，要直接检查两处页面是否生成真实 `table` DOM，且不再裸露 `|---|` 表格语法。

## 原子任务
- [x] P0 | Todo | 新建本轮中文 Todo，并按原子项推进与回填。
- [x] P0 | 链路盘点 | 盘点知识库聊天与 AI 助理当前 Markdown 渲染入口，确认差异点和共用改造面。
- [x] P0 | 共用归一化 | 抽出共用聊天 Markdown 归一化函数，统一处理残缺强调标记和表格块间距。
- [x] P0 | 共用渲染组件 | 新建共用聊天 Markdown 渲染组件，接入 GFM 表格支持、横向滚动容器和统一文本样式。
- [x] P0 | 知识库接入 | 让主题知识库聊天改用新的共用 Markdown 渲染组件和共用归一化规则。
- [x] P0 | AI 助理接入 | 让 AI 助理聊天页和历史会话改用相同的共用 Markdown 渲染组件和共用归一化规则。
- [x] P0 | 去来源呈现 | 去掉两处聊天界面的来源区展示，并把正文里的 `[1][2]` 这类括号引文统一清理掉。
- [x] P0 | 后端提示词收口 | 去掉后台强制来源编号的输出要求，避免新回复继续生成来源括号。
- [x] P0 | 验收脚本 | 新增短时定点验收脚本，分别验证知识库主题页和 AI 助理页的表格渲染结果。
- [x] P0 | 构建验收 | 运行 `npm run build`、既有知识库验收、以及新的 Markdown 定点验收脚本。
- [x] P0 | 复盘 | 判断是否还存在明确结构性问题；若有，则继续开启下一轮中文 Todo。

## 结束标准
- [x] 知识库聊天和 AI 助理回复走同一套 Markdown 解析与渲染规则。
- [x] Markdown 表格在两处页面都渲染成真实表格结构，并支持横向滚动，不再显示原始管道语法。
- [x] 旧的强调修补逻辑没有回退，残缺星号和表格可以同时稳定显示。
- [x] 对话里不再显示来源区，也不再裸露 `[1][2]` 这类来源括号。
- [x] 构建与定点验收通过；若无新的明确提升空间，则本轮结束。
