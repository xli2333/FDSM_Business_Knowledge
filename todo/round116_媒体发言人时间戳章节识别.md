# Round 116：媒体发言人时间戳章节识别

## 对齐结论
- [x] 当前媒体章节识别已经支持 `00:00 开场` 这类行内标题格式，但还没有覆盖真实转录里常见的 `发言人 1 00:00` 头行格式。
- [x] 本轮目标是在不引入复杂 AI 目录生成器的前提下，让媒体工作台能直接从“发言人 + 时间戳 + 后续正文”结构里提取章节目录。
- [x] 本轮继续保持轻量实现：章节标题优先从时间戳后的第一段正文自动推断，重点解决“明明有时间戳却提示未识别”的真实问题。
- [x] 本轮同步收口前端空态提示与真实验收脚本，确保支持格式和用户提示一致。
- [x] 全部 todo 文档与代码改动保持 UTF-8 中文，不引入中文编码错误或 JSX / Python 语法错误。

## 原子任务
- [x] P0 | 后端解析 | 扩展媒体章节提取逻辑，支持 `发言人 1 00:00`、`主持人 00:57` 这类“说话人 + 时间戳”头行格式。
- [x] P0 | 后端标题 | 为头行时间戳补后续正文推断逻辑，自动从下一段内容里抽出简短章节标题，并清理 `↵` 这类噪声字符。
- [x] P0 | 前端提示 | 更新 `MediaStudioPage.jsx` 章节空态文案，明确同时支持 `00:00 开场` 与 `发言人 1 00:00` 两类格式。
- [x] P0 | 测试 | 补后端测试，覆盖“发言人 + 时间戳头行 + 后续正文”上传后立即产出章节。
- [x] P0 | 验收 | 更新 `frontend/scripts/media_acceptance_round106.mjs`，把真实验收文本改成发言人时间戳格式，确保页面能直接显示章节。
- [x] P0 | 验证 | 跑通 `pytest backend/tests/test_admin_editorial_media_features.py -q`、`npm run build`、`npm run media:acceptance:round106`。

## 结束标准
- [x] 上传带有 `发言人 1 00:00` 这类时间戳头行的转录或脚本后，媒体工作台能直接识别章节。
- [x] 章节标题能从后续正文自动推断，不再因为时间戳不在行首标题格式里而整体失效。
- [x] 前端提示文案和真实支持格式一致，不再误导用户。
- [x] `pytest backend/tests/test_admin_editorial_media_features.py -q`、`npm run build`、`npm run media:acceptance:round106` 通过。

## 备注
- [x] 本轮只修复真实转录时间戳格式兼容性，不扩展复杂章节编辑器或 AI 语义分段能力。
